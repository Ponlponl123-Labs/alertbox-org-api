apiVersion: v1
kind: Service
metadata:
  name: alertbox-org-api
  namespace: ponlponl123-labs
spec:
  selector:
    app: alertbox-org-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
  